
const userId : (string | number)[] = ['juuedugt93' , 88373992730747747]
console.log(userId)

//tuple - there is the strict order of the data in the tuple so first the user has to put the value in the same order they are declared

const user:[string , number , boolean] = ['juuedugt93' , 88373992730747747 , true]
console.log(user)

export {}